#include "resource_ids.h"

// Source-only CI builds do not check runtime assets into the repository.
// The asset pack is generated during the build; all other resources are optional
// and should be prepared by a local/release asset setup step when needed.
IDR_ASSETS_BIN RCDATA "assets/assets.bin"

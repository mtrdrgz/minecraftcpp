#include "Item.h"

// All Item methods are currently inline in Item.h. This translation unit exists
// so the build system has something to compile and so future non-trivial logic
// (use(), attack(), getName(), tooltip lines, etc.) can land here without a
// header churn.

namespace mc {

// (intentionally empty for now)

} // namespace mc
